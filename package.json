{
    "name": "wasatchlocals-root",
    "version": "1.0.0",
    "scripts": {
      "dev": "npm run dev --prefix client",
      "build": "npm run build --prefix client",
      "start": "npm run start --prefix server"
    }
  }